# =================================================
# REQUIRED Environment Variables
# =================================================
# These MUST be set for the application to start

# Database connection string (PostgreSQL)
# Format: postgresql://user:password@host:port/database
# Railway Postgres: Will be auto-set if you add a Postgres plugin
# Neon example: postgresql://user:password@ep-example.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql://username:password@localhost:5432/todo_app_dev

# JWT Secret Key (MUST be at least 32 characters)
# IMPORTANT: Generate a secure random secret for production!
# Example: openssl rand -hex 32
# Example: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-at-least-32-chars

# Frontend URL for CORS
# Development: http://localhost:3000
# Production (Vercel): https://your-app.vercel.app
FRONTEND_URL=http://localhost:3000

# OpenAI API Key (REQUIRED for Phase 3 AI Chatbot)
# Get your key from: https://platform.openai.com/api-keys
# IMPORTANT: Keep this secret and never commit it to version control!
OPENAI_API_KEY=sk-your-openai-api-key-here

# =================================================
# OPTIONAL Environment Variables
# =================================================
# These have sensible defaults but can be overridden

# Application environment (development/staging/production)
APP_ENV=development

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=15

# =================================================
# OAuth Configuration (Optional)
# =================================================
# Only required if you want OAuth sign-in

# Google OAuth (https://console.cloud.google.com/)
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth (https://github.com/settings/developers)
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret

# =================================================
# PORT Configuration
# =================================================
# Port for the FastAPI server (configurable via environment)
# - Hugging Face Spaces: Use default 7860
# - Railway: Automatically set by Railway
# - Local development: Use 8000
# PORT=7860

# =================================================
# Deployment-Specific Notes
# =================================================

# Hugging Face Spaces Deployment:
# 1. Create a new Space at https://huggingface.co/spaces
# 2. Choose "Docker" as the SDK (or "Gradio" if using app.py)
# 3. In Settings > Variables, add:
#    - DATABASE_URL: Your Neon PostgreSQL connection string (see below)
#    - JWT_SECRET_KEY: Generate with `openssl rand -hex 32`
#    - OPENAI_API_KEY: Your OpenAI API key
#    - FRONTEND_URL: Your frontend URL (e.g., Vercel deployment)
#    - APP_ENV: production
# 4. The app will automatically start on port 7860
# 5. HF Spaces provides public HTTPS URL automatically

# Neon PostgreSQL Connection (Recommended for Hugging Face Spaces):
# 1. Create database at https://neon.tech/
# 2. Copy connection string from Neon dashboard
# 3. Format: postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
# 4. IMPORTANT: Include ?sslmode=require at the end for SSL
# 5. Example: postgresql://user:pass@ep-cool-dawn-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# Railway Deployment (Alternative):
# 1. Add a PostgreSQL plugin - DATABASE_URL will be auto-configured
# 2. Set JWT_SECRET_KEY to a secure random value (min 32 chars)
# 3. Set FRONTEND_URL to your Vercel deployment URL
# 4. (Optional) Set OAuth credentials if using social login
# 5. Railway will automatically set PORT environment variable
