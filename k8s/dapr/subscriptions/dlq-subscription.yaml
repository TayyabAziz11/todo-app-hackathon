apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: notification-service-dlq
  namespace: todo-app-dev
spec:
  pubsubname: pubsub
  topic: notifications.dlq
  routes:
    default: /events/dlq
  # T074: No retry for DLQ consumer to prevent loops
  bulkSubscribe:
    enabled: false
